class ShopgateWeight {
  /**
   * @param {number} amount
   * @param {string} unit
   */
  constructor (amount, unit) {
    this.amount = amount
    this.unit = unit
  }
}

module.exports = ShopgateWeight
