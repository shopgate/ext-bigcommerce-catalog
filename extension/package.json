{
  "name": "@shopgate/bigcommerce-products",
  "version": "1.0.0",
  "description": "prototype",
  "scripts": {
    "test": "mocha --require ../test/setupBackend.js --recursive ../test/unit -R mocha-jenkins-reporter",
    "testIntegration": "mocha --require ../test/setupBackend.js --recursive ../test/integration",
    "cover": "istanbul cover --dir ../coverage _mocha -- --require ../test/setupBackend.js --recursive ../test/unit"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@stash.localdev.cc:7999/sgx/bigcommerce-products.git"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "stable": "^0.1.6",
    "xregexp": "^3.2.0",
    "node-bigcommerce": "^3.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "sinon": "^4.1.2",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "mocha-jenkins-reporter": "^0.3.7",
    "nock": "^9.0.13",
    "request": "^2.83.0"
  },
  "standard": {
    "ignore": [
      "/products/sortCharacteristics.js"
    ],
    "env": {
      "mocha": true
    }
  }
}
